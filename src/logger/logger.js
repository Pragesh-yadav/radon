const name = "Pragesh Yadav"
const batch = "Radon"

const welcome = function() {
    console.log('Welcome To my application. I am ' + name + " " + 'and a part of FunctionUp' +" "+ batch + ' cohort'  )
}

module.exports.name = name
module.exports.batch = batch
module.exports.welcome = welcome